# Cloudflare Worker Configuration for Media Gateway
# Domain: media.camly.co

name = "media-gateway"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# R2 Bucket Binding - không cần access key/secret
[[r2_buckets]]
binding = "MEDIA_BUCKET"
bucket_name = "cloudshare"

# Custom domain routes (cấu hình thêm qua Dashboard)
# [routes]
# pattern = "media.camly.co/*"
# zone_name = "camly.co"
